import { Layout } from "../layouts/Layout";
import { Box } from "@mui/material";

export default function NotFound() {
  return (
    <Layout>
      <Box sx={{ p: 4, margin: "auto", alignContent: "center" }}>404</Box>
    </Layout>
  );
}
